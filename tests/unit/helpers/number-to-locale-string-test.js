import { numberToLocaleString } from 'parish-app/helpers/number-to-locale-string';
import { module, test } from 'qunit';

module('Unit | Helper | number to locale string');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = numberToLocaleString([42]);
  assert.ok(result);
});
